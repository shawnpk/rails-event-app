<h1><%= @events.size %> Events</h1>
<ul>
  <% @events.each do |event| %>
    <li>
    <strong><%= event.name %></strong>
    in <%= event.location %>:
    <% if event.price == 0 %>
      Free
    <% else %>
      <%= number_to_currency(event.price) %>
    <% end %>
    </li>
  <% end %>
</ul>